> 2026-09-08 范围修订：用户明确允许新规则正式试运行，效果继续验证。本轮实施与部署以 [trial-cutover.md](trial-cutover.md) 为准；下方原独立验证任务保留记录，不冒充已完成，也不再作为本轮 trial 切换前置条件。

## 1. 阶段 A：事实契约与兼容底座

依赖：无。对应 auditable-market-facts、versioned-opportunity-engine。进度随实施更新；勾选表示产物和验证均完成，不能仅凭提案文本勾选。

- [x] 1.1 核对当前迁移号、client/context/scheduler、discovery、quote/gate及outbox调用边界，固定legacy风险策略/成本语义/行为回归fixtures；产出模块映射和兼容基线清单。
- [x] 1.2 在`src/gmgn/facts.ts`定义允许字段清单、单位/来源/语义版本及fact envelope；用脱敏真实响应验证支持矩阵，未知Info价格时间输出UNVERIFIED，覆盖错误单位、零分母和无独立买家字段。
- [x] 1.3 扩展context purpose和物理transport attempt关联，持久化真实queued/requested/received时刻；验证排队与重试不会重写物理时刻、缓存不增加确认事实、凭据不进入记录。
- [x] 1.4 增量创建D3事实、母集、运行、决定、基准、任务、数据用途及凭据表与唯一约束；测试旧库迁移、重复写、并发CAS、事务回滚及旧冻结snapshot不变。
- [x] 1.5 在旧市场门之前接入公共母集，实现稳定分层采样、热集20与排除账本；验证旧证据拒绝者仍可被研究、安全拒绝者不能报价或推送、模型不能改变采样优先级。
- [x] 1.6 实现事实归档/校验/ID定位与7天、2GiB保护；验证清理后回放可复算、满盘停止研究但保留正式数据、run引用超限终止且不删困难样本。
- [x] 1.7 实现全局token投递锁、decision_format/publisher_version隔离与单publisher租约的legacy兼容层，登记最低回滚镜像契约；测试新格式READY不被legacy消费、UNKNOWN跨池/重启仍锁定、双进程不能同时发布。
- [x] 1.8 增加默认research off的配置及只读`audit`命令，输出JSON/Markdown支持和覆盖报告；Node24执行`npm run ci`，验证旧配置/原调用/发送快照行为不变，交付阶段A兼容构建和报告。

## 2. 阶段 B：统一机会与离线回放

依赖：第1组完成；对应 versioned-opportunity-engine、auditable-market-facts。

- [x] 2.1 实现完整model manifest schema和有限表达式解释器，字段支持/TTL/数值/谓词/版本必须齐全；缺重置、不支持特征、任意脚本和非有限值拒绝加载。
- [x] 2.2 实现纯evaluate与D4状态机，固化anchor及activation_fact、CAS状态和明确reset；测试同缓存、恢复、取消和报价刷新不新建机会，池迁移失效且不解除投递锁。
- [x] 2.3 实现legacy市场判定回放适配器和统一逐层reason输出；冻结相同风险策略，研究不以旧市场门为前置，比较只使用评价时已返回事实。
- [x] 2.4 实现`manifest validate`和无网络`replay`、事实ID/归档解析及确定性报告；同输入两次回放决定/样本身份一致，迟到事实不能改变旧决定，多模型不新增API或Telegram流量。
- [x] 2.5 实现新DecisionContext与最终风险/入场复核的干运行适配，准备失败取消原机会而非换路线；使用mock验证卡片/outbox格式及冻结行为，正式publisher仍legacy。
- [ ] 2.6 完成新publisher适配器干运行路径、策略版本/模板及配置验证，在最终测试前冻结；测试validated无效凭据启动失败不回退、legacy旧配置有效、新模式旧市场键报错，真实Telegram发送保持禁用。
- [x] 2.7 实现部署预检/排空准备/独占租约/最低回滚镜像检查，用fixtures演练新发送/UNKNOWN后切兼容legacy、异格式未完成任务隔离与DB不降级，登记可重复的回滚验证记录。

## 3. 阶段 B：配对基准与结果测量

依赖：第1组、2.1–2.3完成；对应 paired-signal-baselines。

- [x] 3.1 实现三轨baseline schema/协议哈希、PENDING先落库、首有效响应CAS及5s总期限/物理attempt约束；完整执行模式中准备未完成为MISSING_BASELINE原因MISSING_PREPARATION，另实现阶段C决定时诊断协议，终态不重定价且协议不混算。
- [x] 3.2 接入真实确认与固定1000ms SIMULATED确认采样，严格检查Info来源时间及Quote同数量；验证卡片1.00/确认后1.30只涨到1.31不算主轨道1.3x，逻辑1s/物理6s拒绝基准，5s后重启只记缺失。
- [x] 3.3 实现1.3/1.5/2/3倍与-10%先触达、边界/同K线顺序不明、NOT_TOUCHED/CENSORED/UNKNOWN的互斥结果；验证先跌后涨为SL，未知不插补，24h到期不判亏。
- [x] 3.4 实现共同24h观察、分段cadence、token/pool/range复用、200待办上限和最多3次捕获的持久化任务；验证错过/重试不移动观察坐标、超预算保留排除记录。
- [x] 3.5 实现确认后Quote买入数量配对退出及准备成本独立报告；验证K线2x不能代替卖出Quote、晚到退出只报实际时点、旧逻辑时间报价不能混入有效基准。
- [x] 3.6 实现各轨道逐目标全部分母、条件率、缺失/覆盖/等待报告；用合成账本验证类别互斥且计数守恒，旧60min/card与新24h/协议版本不能混算，输出阶段B测量可行性报告。

## 4. 阶段 B：共享预算与执行隔离

依赖：1.3、1.4完成；接入测量在第3组后验收。对应 bounded-shadow-experiments。

- [x] 4.1 在现有scheduler同一临界区实现研究2权重/s补充、容量5空桶、滚动60s 120、全局20/正式reserve及逐attempt扣费；虚拟时钟验证2.5s后可发权重5、跨用途合计不超额、拒绝准入不扣费。
- [x] 4.2 实现off/observe/collect/execute_shadow模式与独立预算清单，研究并发1、单影子在途/待办限制、Quote6次/min及2s物理超时；验证off无新行为、observe无新增请求、对照补采也计父预算。
- [x] 4.3 实现正式待办/在途、等待、429/cooldown与监控失败的暂停及60s恢复条件；测试影子买腿在途时正式到达、卖腿让路、学习间隔使最坏阻塞超过3s时禁用研究Quote。
- [x] 4.4 实现完整Quote复用键和逐腿准入/过期处理，保留RESOURCE_EXCLUDED原时刻；覆盖同token不同数量、不同pool/wallet/slippage及迟到缓存不可复用。
- [x] 4.5 实现`budget-check`与同一到达流开/关研究的mock负载比较，报告P50/P95/P99及最大新增等待；超3s或缺测量阻止execute_shadow，保存预算证据哈希，运行中越限自动暂停。

## 5. 阶段 C：有限研究与模型选择

依赖：第1–4组完成且市场基准支持/预算报告通过。对应 independent-strategy-validation。数据不足可结束本阶段研究，但不能标记模型已晋级。

- [x] 5.1 实现数据用途台账和`dataset freeze`，按chain+token跨池分组并锁定截止/成熟标签；测试开发token换池不能入最终集、归一化/回看/未来标签不泄漏、已消费数据不能重作最终证明。
- [ ] 5.2 实现候选生成/登记工具与开发报告，基于开发集显式分位点或业务比较点生成最多3方式×4组合的完整manifest；固定样本起点、第一市场合格机会与参数推导，无可支持参数时给出失败原因。
- [ ] 5.3 在新选择集登记后离线运行有限候选和相同legacy对照，按D7覆盖/样本/捕获预筛及排序实现`select`；测试零分母、少量全赢、并列hash排序、执行缺失不能当通过，无候选时NO_PROMOTABLE_MODEL。
- [ ] 5.4 交付选择报告与唯一模型包（或明确未晋级报告），冻结模型/风险/基准/资源/评价清单及语义构建依赖哈希；人工核对生成的具体筛选规则和所需GMGN字段，任何改动重新形成对应版本证据。

## 6. 阶段 D：独立最终验收工具与运行

工具开发依赖第1–4组；最终数据运行必须等5.4选出唯一模型。对应 independent-strategy-validation、bounded-shadow-experiments。

- [x] 6.1 实现预登记30天采集+24h成熟+2h补采、一次解封评价、独立token/固定对照/停止规则；提前按收益调参或选停止点使数据消费，样本不足不自动延长。
- [x] 6.2 实现共同母集token成对bootstrap 10000次及固定seed、1.3x/2x/捕获/SL联合门；覆盖双方未触发和准备失败保留分母、两组分别达100token/80%、无效界限输出INCONCLUSIVE。
- [x] 6.3 实现缺失成功区间与1.3x保守敏感性门，以及10U准备成本中位数/P95差值上界≤0.01、准备失败差值上界≤0.02；合成数据验证仅覆盖改善不能晋级、成本退步不能以市场TP抵消。
- [x] 6.4 实现`evaluate-final`与`promotion verify`，保存全部版本/代码依赖/数据状态哈希、PASS/FAIL/INCONCLUSIVE及消费状态；变参数、风险、准备语义或预算均不能沿用旧凭据，纯文档改动不误作模型改变。
- [ ] 6.5 在取得阶段B负载证据、2.6–2.7冻结干运行/兼容构建、6.1–6.4验收工具及阶段C唯一模型后，登记最终run并按本地→GitHub→SEA流程启用协议内采集；核对现有服务与研究健康/覆盖，收益在截止前不作为调参依据，保留完整运行清单。
- [ ] 6.6 run按预定期限关闭并成熟后执行唯一最终评价，交付未删缺失样本的报告与有效凭据或失败终态；未通过时停止进入阶段E，不以完成本任务宣称策略有效。

## 7. 阶段 E：有条件切换、清理与交付

依赖6.6获得匹配PASS凭据；干运行与兼容工具已在2.6–2.7交付。对应 versioned-opportunity-engine及全部兼容契约。

- [ ] 7.1 在模型通过后生成本地配置变更和可审阅发布清单，核对冻结构建/全部语义哈希与最终凭据，运行Node24 `npm run ci`及迁移/混合负载/恢复必要检查；按本地提交→GitHub→SEA拉取→Compose激活，不迁移本机DB、不上传凭据。
- [ ] 7.2 查看真实运行的漏斗原因、采集覆盖、正式延迟/429、基准冻结与防重复状态，按登记回滚规则处置；测试消息不发送，任何新增通知遵守用户授权范围。
- [ ] 7.3 切换验收后单独删除旧正式资格路线/评分/旧影子叠加入口，保留版本化历史解析、只读legacy对照与已登记兼容镜像；复核无双份新参数所有权，交付运维与调参文档。

## 8. 交付判据

以上共39项实施任务，完成情况以勾选及implementation.md为准。A/B工程可在提案通过后开始；C/D是有失败出口的数据工作，E是有条件的激活。阶段未通过时，下游任务保留未完成并记录阻塞证据；不能通过勾选“跳过”伪装整个策略整改完成。工具完成与市场验收成功分别记录。
