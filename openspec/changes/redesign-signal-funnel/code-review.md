# 2026-09-08 本批 review 与修复

本批按正确性、预算/安全、数据一致性、兼容回归和测试有效性审查。以下问题均已在本地修复后回归；未宣称整个新正式发布路径已经完成。

| 级别 | 发现与影响                                             | 修复及验证                                                                                   |
| ---- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| P1   | 研究采样器次数限制之外，底层网络重试会放大真实请求次数 | client研究请求取消隐式重试，attempt明确research；runtime网络错误测试证明研究一次、原正式两次 |
| P1   | 准备失败仍生成SIMULATED确认，污染主基准分母与入场坐标  | 失败保存NULL确认和MISSING_PREPARATION；capture测试验证无API、不可改写成后续确认              |
| P1   | 安全检查前的市场READY可能被当作合格信号统计            | 准备适配器在初次安全通过后才保存qualification；初始安全失败不报价，执行失败仍留在分母        |
| P1   | 准备完成后写库/重启可能推迟虚拟确认并重新取价          | preparedAtMs进入不可变准备记录；恢复固定确认并终结缺失，runtime恢复测试不发网络请求          |
| P1   | “实际USD必须精确等于10”会拒绝原子数量换算后的正常报价  | 名义10U与真实返回成本分离；协议v2，9.999999U同数量退出测试保留实际收益                       |
| P1   | 全部风险数据再次重拉可能导致最终报价过期               | 复用仍在原TTL内的深度数据，最终刷新Info/security/pool；过期/池变/风险失败仍取消              |
| P1   | 自填选择数据、变动对照或统计JSON不能构成独立晋级证据   | 候选值溯源、选择前固定风险/对照/代码、DB冻结账本最终评价、一次消费及哈希匹配测试             |
| P2   | 市场任务与Quote退出分别计数可能超过共同200上限         | 两类任务共享计数；保留RESOURCE_EXCLUDED原坐标                                                |
| P2   | 原Traders基线包含非标签钱包会造成虚假的覆盖变化        | 通过同一assessCoordinatedExit标准化快照                                                      |
| P2   | 被范围缓存引用的事实可能在维护时被删除                 | 归档维护保留range引用；缺口/质量不合格事实不作为完整缓存                                     |
| P2   | 仅哈希类toString遗漏外部预算/安全辅助实现              | 预算哈希包含完整依赖文件；语义构建递归包含依赖及锁文件，Docker保留package-lock               |

尚存的实施边界：2.6的完整新正式发布配置与运行绑定未交付；有效匹配凭据的真实新发布路径不能仅由当前dry-publisher推出。故保持该任务未勾选，validated启动失败而不回退。6.5的最终对照采集运行也未启用；当前ACTUAL回调属于独立测量组，不能伪装成执行准备前的完整对照分母。阶段C实际研究和D/E仍未完成。

本批已实现路径的检查与回归结果见implementation/deployment；不存在通过省略缺失样本、假造PASS或发送测试通知完成验收的行为。

---

# 实施代码审查（2026-09-07）

审查范围：b55f025 之后本地全部源码、迁移、配置和新增测试；属于当前代理自查，不是独立第三方审计。

## 已修复 findings

- [P1] Telegram 成功之后本地确认异常可能释放 token 锁，造成重复推送。`src/delivery/outbox.ts:109` 的发送异常分类现在区分请求已开始和服务端已成功；落库失败归入 UNKNOWN，保留锁。测试模拟成功响应后 SQLite 异常，验证第二轮不重发。
- [P1] 非 Info 响应不带池地址时落为 unresolved，回放无法关联同池 K 线；晚到响应也可能被错误归入新池。`src/gmgn/client.ts:192` 在物理请求开始时绑定已观测池，`src/gmgn/facts.ts:162` 只在响应没有明确池地址时使用绑定值，已有事实不重标。
- [P1] READY 使用同一缓存价格时，时间窗口改变可能使入场条件失效，却提前返回 NO_NEW_FACT。`src/decision/opportunity.ts:122` 现在允许时间变化使缓存机会失效，仍不把缓存当新增确认；反例测试验证锚点不漂移。
- [P2] 大批发现事件与物理事实共用 200 个待写名额，单批 300 个候选可能使观察立即停止。`src/research/recorder.ts:68` 将发现事件放入有界队列，每次仅提交一个串行写操作，让正式写入可以穿插；溢出仍显式终止研究，不静默丢弃困难样本。
- [P2] 同 attempt/id/hash、但时刻被改变的重复写未报错。`src/research/storage.ts:134` 现在比较完整 envelope，禁止物理身份漂移。
- [P2] 列表裁剪未标记数据不完整。`src/gmgn/facts.ts:162` 现在输出 ARRAY_TRUNCATED / VALUE_TRUNCATED，回放不能把被裁剪输入当完整事实。
- [P2] socket 空闲超时不保证研究请求的总物理耗时。`src/gmgn/client.ts:192` 为新增研究请求增加 2s 总期限，Quote 不重试；正式已有超时配置不变。
- [P2] 无界回放输出与大窗口 Decimal 展开可能耗尽内存或参数栈。`src/research/replay.ts:106` 增加报告容量失败出口；模型窗口聚合改为迭代。

## SEA 核验后的追加修复

- [P1] 真实发现响应使用 rank、分组 tokens 或直接数组，研究记录器仅读取 list，导致已记录物理 attempt 的 payload 为空。`src/gmgn/facts.ts` 现在复用 `src/discovery/adapters.ts:260` 的现有解包逻辑，再应用允许字段清单；正式发现与筛选逻辑不变。新增四类结构的回归测试，证明字段保留和敏感字段排除。
- Docker 构建中递归 chown 已复制的依赖造成额外文件层写入；改为先创建相同用户，再 COPY --chown，最终权限保持一致。已通过本地镜像构建与非 root 冒烟测试。

## 研究门禁的最终边界复核

- [P2] 监控从故障恢复时沿用上次故障开始时间，会提前恢复新增研究。`src/gmgn/scheduler.ts` 现在从健康恢复时刻重新累计60秒，同时禁止在非登记的paced 14/20 + reserve6策略下启用研究。
- [P2] 冻结计划可以声明未来时刻而绕过成熟期限。`src/research/dataset.ts` 现在拒绝未来冻结时间。对应回归测试通过。

## 未完成范围与发布判断

- [P2，已修复] `src/research/measurement.ts` 将到期但没有完整行情的样本一律记 UNKNOWN，不符合 D7 的截断定义。现区分无已观测触达的 CENSORED 与缺口后发生触达的 UNKNOWN；缺失尾部不计亏损。补充回归用例及独立注册分母报告，重复/改坐标/无效基准成功均拒绝。

本批允许发布被动 observe 及离线工具。未发现阻止这一范围发布的未修复 P0/P1。

该结论不覆盖 collect / execute_shadow、真实配对采集、新发布适配器、最终 run 与晋级凭据。这些入口尚未完整交付，配置继续拒绝启用；干运行接口和合成统计结果不能冒充在线验证。任务按实际验收勾选，未把整个提案标记完成。

结构化限制：Info 无已验证价格来源时间；历史卡片和旧 60min 结果不能提供新 24h 确认后基准证明。需要先解决/重新审查该数据契约，再完成真实配对测量和后续模型研究。工程遗留仍单独列在 tasks.md，不能一概归因于市场样本等待。
