# 2026-09-08 无信号运行故障修复

## 问题与范围

SEA 于 16:08 UTC 仍运行 legacy live + research observe；最后 SENT 为 9 月 7 日 16:46 UTC，旧影子评估最后更新为 9 月 8 日 00:17 UTC。最近六小时有 885 次泛化处理失败，最近日志对应 task deadline expired；GMGN 有 75 次 401、147 次超时，无 429。

研究记录逐事件调用 dbstat，同步遍历约 749 MB 研究页面，线上单次测量约 472 ms。50 条候选重复扫描就可能消耗约 24 s。研究记录虽不增加 API 请求，仍阻塞正式业务的事件循环。这是此前隔离实现和验证遗漏。

本次不修改筛选阈值，不开启新策略，不改写历史信号卡片、确认价格或结果数据。提案进度仍为 30/39，新正式发布器与独立验证尚未完成。

## 修复

- 被动记录启动时在 worker 中测量容量，逐事件/事实使用保守增量预留，每分钟由 worker 校准；校准保留采样开始后所有新增预留，失败不释放预留。重复物理事实不重复计费，已知候选只预留固定字段更新开销。保持容量限制与明确的停止原因。
- 归档文件读写和页面扫描转移至 worker；维护不重叠、有 30 s 上限。研究事件逐条让出事件循环，退出时停止待处理队列；校准不可用只停止研究记录，不终止正式进程。
- 采样重排复用 prepared statement，消除逐行重新编译 SQL。
- 正式 GMGN 请求将共同截止时间传入物理传输，排队消耗同一时间预算；握手阶段同样受绝对超时限制，过期成功响应不得当成新鲜数据。排队过期记录为 queue_timeout，响应中断及时释放在途资源。
- 非 2xx 记录脱敏的响应分类、请求标识、服务端日期和响应类型；不保存任意响应正文或凭据。401 的来源需要部署后证据，不能仅凭状态推断密钥无效。
- 健康文件与分钟指标增加事件循环延迟、候选安全处理进度、影子更新时间和端点最后成功时间。Docker 健康检查同时检查业务 degraded 状态；没有信号本身不判故障，持续数据停滞和事件循环阻塞才判故障。

容量预留仍是保守估算，校准使用实际研究表/索引页及登记归档字节。非记录器的离线研究工具继续进行实际容量检查。本次线上验收针对当前 observe 模式，不代表 collect/execute_shadow 的完整负载或晋级验证通过。

## Review 与本地验证

已检查共享写队列、校准期间并发写入、失败/关闭、重复事实、请求重试与截止时间、敏感响应脱敏、历史发送兼容性。修正了重复事实重复预留与 worker 意外退出导致 Promise 未完成的问题。

- 完整 Node 24 CI：340 项测试通过，类型检查、lint、构建通过。
- 接近线上规模的独立测试：750 MiB JSON 事实 + 10,000 条历史候选；100 条新候选批次 56.5 ms，I/O 定时器运行 17 次。测试显式禁止热路径调用 estimatedBytes，避免仅以计时掩盖重复扫描。
- 覆盖容量校准时的新增预留、额度不足拒绝写入、网络握手卡住时的绝对超时、迟到成功响应拒绝、401 脱敏与业务健康退化/恢复。
- 本地网络测试仅监听回环地址，不请求 GMGN，不发送 Telegram 测试消息。性能临时数据库在测试后清理，不进入 GitHub。

部署结果在验证后补充；不能将小规模单元测试或容器存活等同于正式链路恢复。

## 首次上线复验发现的第二处瓶颈

00255c0 于 16:36 UTC 部署后，54 秒内安全检查完成 22 次，旧影子决策恢复新增；CPU 一次采样 25.13%，该窗口 401 为 0。但主线程仍有秒级延迟。16:38 UTC 只读测量确认 retainAudit 的事件保留查询在 eligible=0 时仍全表扫描，耗时 1490.5 ms。

已在本地追加 019 索引迁移，为过期时间、来源快照恢复查找和 Episode token 关联建立索引；恢复查找显式使用 chain。查询计划回归保证不存在 SCAN e 或临时自动索引，原有保留/保护历史样本的语义测试继续执行。本批不删除业务数据，部署前为现有 SEA 数据库做一致性备份。

## 事务提交复验与追加修复

第二轮上线后，清理选择查询实测 0.994 ms，影子评估继续新增，但仍有主线程延迟。10 s V8 调用栈采样中约 6.3 s 落在 SQLite 事务原生 run，故继续修复写放大：重复 event_key 在事务内只读返回，不再反复写 token 时间戳；研究宇宙采用最多 50 条的小批量原子提交，并在批次之间让出 I/O。正式 Episode/Outbox/Telegram 确认的提交机制未合并、未降低 SQLite 同步持久化等级。新增回归验证重复事件不产生任何数据库变更、研究批次上限和注入错误时完整回滚；最终完整 CI 343 项通过。

诊断操作曾造成额外中断：16:50:05 UTC，临时 Inspector 清理命令中的动态 import 在该执行上下文缺少回调，产生未处理异常；16:53:02 UTC 已用原镜像及原数据卷恢复。后续改用已本地验证的同步内置模块获取方式，采样正常结束。此中断由诊断操作造成，不归因于筛选代码；没有修改已发送数据。

## 采集批次的独立恢复

8afd311 部署后发现旧 research-observe-v1 已为 INCONCLUSIVE，研究事实最后写入 16:55 UTC；独立容量测量只有 807,391,232 字节、归档为 0，未达 2 GiB 上限。因此，采集停用后的 CPU 数值不能用来证明批处理在真实采集负载下通过。旧容器日志已随重建移除，无法从旧版泛化状态还原确切停止原因，不作推断。

保留旧批次及事实，Compose 默认新建 research-observe-v2-runtime-fix 被动采集批次，仍保持原 2 GiB 上限和 legacy live。研究停止原因现在写入 operation_traces 的 research_failure 记录，维护错误区分阶段与脱敏 SQLite 错误码，以便跨容器重建诊断；不将已终止批次改回 ACTIVE。最终验收必须在新批次 ACTIVE 且持续写入时进行。

## 自动检查点隔离

研究持续开启时的复验仍见周期性提交延迟，V8 采样热点落在 sqliteTransaction 的原生提交调用。SQLite 默认由触发阈值的提交线程执行自动检查点，官方支持将周期检查点转移到独立线程（[SQLite WAL 性能说明](https://www.sqlite.org/wal.html#performance_considerations)）。因此追加后台 PASSIVE 检查点，并保留 FULL 持久化；主连接只在后台线程就绪后取消自动检查点，后台失败或心跳丢失恢复原自动机制并明确标为故障。健康记录包含检查点耗时、WAL 帧数与未回写积压，积压超过 256 MiB 或后台故障导致健康检查失败。

新增测试验证并发读事务阻挡检查点时不影响已提交内容，读事务释放后积压消退，关闭时恢复自动机制，FULL 同步等级始终不变。SQLite 本地实际版本 3.53.2，已高于官方 WAL-reset 修复版本；没有降低持久化等级或删除 WAL 文件。

## 630a33f 最终部署

- 按本地修改/Review/测试 → GitHub → SEA `/www/wwwroot/0xbsc` 拉取 → Docker Compose 部署完成，应用提交 `630a33f`。
- 最终镜像容器于 2026-09-08 17:20:40 UTC 启动；容器 `6aedad82ebcb`，容器报告的 `.Image` 为 `sha256:7b987e7631e4f56d290cf561067fcff3bc1983b2508a3cc2a18969f42d4b3893`。
- 原数据库保留；索引迁移前一致性备份 `/var/lib/gmgn-signal-bot/backups/pre-e349ac9-cold.sqlite`，6,601,220,096 字节、0600。保留 `bsc-meme-signal-bot:pre-630a33f` 等兼容回滚镜像。未上传凭据、数据库或调试文件到 GitHub。
- 完整本地 CI 与镜像内 CI 均为 345 项测试通过；真实规模独立压力测试为 750 MiB 事实和 10,000 条历史候选，100 条新候选批次 43.7 ms。
- 首个上线复验（17:21:43 UTC）：研究批次 ACTIVE，无研究失败；安全检查完成 56 次，候选排队超时 0，影子决策新增 30 条，研究事实新增 261 条。最近窗口 261 次 API 调用全部成功，无 401/429/超时；P95 请求耗时 328 ms，P95 排队 1519 ms。
- 检查点在后台正常运行，已回写 7909/7909 帧，积压 0，最近检查点耗时 369 ms；同一健康采样事件循环 P99 为 153 ms。CPU 单次采样 10.91%，研究确实启用，因此没有使用停用采集后的 CPU 数据作为修复证据。
- 正式模式仍是 legacy live，研究为 observe，新正式筛选尚未启用；正式配置 revision 保持原值。首个复验时 SENT 17、发送前取消 56，没有手动测试推送。

### 跨维护周期验收（17:24:01 UTC）

- 容器 healthy，重启计数 0；研究批次 ACTIVE、无 research_failure。
- 本次启动后物理请求 871 次，全部 200/success；401、429、传输超时均为 0，候选 queueTimeouts 为 0。
- 安全检查完成 199 次；研究事实新增 871 条，旧影子决策新增 123 条且继续更新。
- 最近一分钟请求 P95 耗时 239 ms、P95 排队 878 ms；事件循环 P99 300 ms。仍观察到一次窗口最大延迟 1.86 s，不能声称所有延迟已消除，但该窗口无请求超时或候选队列过期。
- 后台检查点正常，17174/17174 帧完成，未回写积压 0；本次回写耗时 71.7 ms。线上 SQLite 实际版本同为 3.53.2。
- SENT 仍为 17；本轮修复开始后的新增 6 条发送前取消中，报价拒绝 3 条、路由/安全复核失效 3 条。没有实际 Telegram 发送失败；取消与成功推送分别统计，没有为了产生消息而放宽条件或补发测试消息。

结论：本次已恢复候选数据获取、影子评估和持续研究记录，并修复已确认的重复扫描、事务写放大、请求期限与检查点阻塞问题。871 次请求的短窗口不代表服务商永远不会出现鉴权/限流问题，也不代表新策略的收益验证或晋级已经完成。新规则正式发布和其余提案任务仍按原独立条件处理。
